# 2026-07-11 工作日志

## Phase 2 三项修复

### 1. Phase 2 聊天头像改为文字头像
- **问题**：每次进入 phase2 聊天都要重新加载图片头像
- **方案**：弃用图片头像，改用单字文字头像（鹿/夜/远/辰）
- **改动文件**：
  - `Echo/src/features/onboarding/v2/onboarding-v2.types.ts` — RoleAgentDef 新增 `avatarText` + `avatarColor` 字段
  - `Echo/src/features/onboarding/v2/roleplay-agents.data.ts` — 移除 avatarUrl，添加 avatarText(鹿/夜/远/辰) + avatarColor(各自色号)
  - `Echo/src/features/onboarding/v2/components/ChatBubble.tsx` — 从 `<img avatarUrl>` 改为 `<div avatarText+avatarColor>` 圆形文字头像
  - `Echo/src/features/onboarding/v2/components/RoleCard.tsx` — 同上，选择卡片头像改为文字
  - `Echo/src/features/onboarding/v2/Phase2Roleplay.tsx` — Header 头像 + ChatBubble 调用改为传 avatarText/avatarColor

### 2. 小鹿 agent 聊天 bug — 全部撤回，不修复
- 用户指出：数据库部署在服务器 Docker 上，不是 Neon，我之前查的是 Neon 旧数据，分析前提全部错误
- 用户的假设：用户注册两次，第一次和小鹿聊过且点了结束对话，旧 roleplayChats 通过 `{ ...existingSurvey, identity }` 保留；第二次注册到 phase2 时旧对话数据导致问题
- 所有相关修改已全部撤回，恢复原始代码：
  - `roleplay-agent.service.ts` isFarewell 恢复原样（保留"我去"关键词）
  - `Phase2Roleplay.tsx` handleSend 的 `!res.ok` 和 `data.ended` 分支恢复原样（保留 `setConversation(null)` + `setSelectedRole(null)` 弹回逻辑）

### 3. Phase 1 结束后加载动画在手机上显示过大且不居中
- **问题**：Phase1_5Sketch（人格画像）和 Phase1_6IdealSketch（理想伴侣）加载动画在手机上溢出屏幕且不居中
- **根因**：loading 状态容器缺少 `max-w-[375px] mx-auto text-center`，且 LottieLoader 没有 `flex justify-center` 包裹（同文件 error 状态有这些 class，loading 状态遗漏了）
- **修复**：
  - `Echo/src/features/onboarding/v2/Phase1_5Sketch.tsx` — loading 容器加 `max-w-[375px] mx-auto text-center`，LottieLoader 包裹 `<div className="flex justify-center mb-1">`
  - `Echo/src/features/onboarding/v2/Phase1_6IdealSketch.tsx` — 同上
  - `Echo/src/features/clone/IdealPartnerSetup.tsx` — 同上（bonus 修复）
- **对比参考**：Finalize.tsx 和 PersonaFragment.tsx 的加载动画显示正常，因为它们有 `max-w-[375px] mx-auto` + `flex justify-center` 包裹

## 验证
- 前端 `npx tsc --noEmit`：退出码 0（3 个 pre-existing poster 模块报错与本次修改无关）
- 后端 `npx tsc --noEmit`：退出码 0，无错误
