## 2026-06-23

### 回声精灵表情图生成
- 使用 Wan2.7-Image-Pro (DashScope) 通过 `mcp__wan-image__generate_image` 工具，基于 `Echo/logo/echo_boy.png` 的角色设计，文生图生成9张表情
- 因 `edit_image` 需要公开URL（localhost 无法被 DashScope 服务端访问），改用 `generate_image` 配合详细中文描述
- MCP connector 间歇性断开，需重试恢复
- 生成结果保存在 `Echo/logo/icons_gender/`：
  - echo_boy_惊讶.png / 害羞.png / 大笑.png / 耍酷.png / 思考.png / 困倦.png / 生气.png / 庆祝.png / 得意.png

### MCP dashscope_client 改造：支持本地文件路径
- 新增 `_local_path_to_data_uri()` 函数，将本地文件自动转为 base64 data URI
- 修改 `edit_image()` 对每个 input_image 调用该函数，兼容三种输入：公网URL / 本地路径 / data URI
- 已测试通过：直接用 `C:/Users/.../echo_boy.png` 本地路径调用 edit_image 成功

### 回声精灵表情图 v2（图生图 + 英文命名）
- 使用 edit_image + 本地 echo_boy.png 参考图，生成9张表情，角色一致性明显优于纯文生图版本
- 保存在 `Echo/logo/icons_gender/`，英文文件名：
  - echo_boy_surprised.png / shy.png / laughing.png / cool.png / thinking.png
  - echo_boy_sleepy.png / angry.png / celebrating.png / proud.png
