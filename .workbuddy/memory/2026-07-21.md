# 2026-07-21 工作日志

## Onboarding 登录恢复 + Phase2 聊天改进审查报告

用户提出两项 Onboarding 改进诉求，做了"只审查不修改"的代码探索并产出改进报告：

## Phase 2 角色扮演两个 Bug 修复

用户报告 Phase 2 聊天中两个问题：
1. 小鹿的"等等我想说"反复出现（不完整句子）
2. 阿远错别字概率过大（"哪就好"/"感绝"）

**根因**：
- Bug1：`roleplay-agent.service.ts` 的 `generateFollowUp()` bestfriend 池子含 `'等等我想说'`（不完整片段）
- Bug2：错别字注入是全局机制（所有角色），但只有小鹿的 prompt 写了"偶尔打错字"；同音字映射含 那↔哪、觉↔绝 高干扰配对；概率 5% 偏高

**修复**（`roleplay-agents.ts` + `roleplay-agent.service.ts`）：
- 最初加了 `typoEnabled` 角色门控（仅 bestfriend），后根据反馈回退：所有角色都可以打错字，只降概率
- `typo` 概率 0.05→0.03（全角色）
- 移除 HOMOPHONE_MAP 中 那↔哪、觉↔绝（高干扰）
- bestfriend followUp 池 "等等我想说"→"笑死"
- tsc --noEmit 退出码 0
`outputs/onboarding-resume-and-phase2-chat-improvement-report.md`

### 审查结论

**诉求 1：登录后弹窗提示上次进度 + 允许跳回已完成 Phase 修改** — 属实
- `Echo/src/features/onboarding/v2/OnboardingShell.tsx:47-129` 登录恢复完全静默，无弹窗
- `OnboardingShell.tsx:132-176` 状态机只有 `advancePhase`（前进）和 `goBackToPhase`（仅错误回退）
- 无用户主动跨 phase 跳转入口
- 后端 `submitPhase0`/`submitPhase1` 已是 upsert，数据层支持回改，仅缺 UI
- 关于"不要显示 phase0/phase1 字样"：UI 实际不显示这些内部代号，但确实缺统一的"phase 总结性标签"导航体系

**诉求 2：Phase2 聊天连发 2 条消息，agent 回复插在中间** — 属实
- 根因：`Phase2Roleplay.tsx:552` API 完成后立即 `setApiPending(false)`，但 assistant 消息还在 `pendingDisplayIds` 隐藏中（默认 800ms 起跳逐条揭示）
- `Phase2Roleplay.tsx:406` 发送门控只看 `apiPending`，不看 `pendingDisplayIds.size`
- 用户在窗口期发新消息 → 新消息 push 到已隐藏 agent 回复之后 → agent 回复揭示后插在中间
- 后端 `roleplay-agent.service.ts` 顺序正确，无需改动

### Phase 标签体系（建议）

| phase | 建议标签 |
|-------|---------|
| phase0 | 基础信息 |
| phase1 | 个人特征 |
| phase1_5 | 人格画像 |
| phase1_6 | 理想型 |
| phase2 | 角色对话 |
| finalize | 分身孵化 |

### 实施计划（分 5 阶段）
- A：Phase2 顺序修复（改 `Phase2Roleplay.tsx:406`+`872` 加 `pendingDisplayIds.size > 0` 门控）— 独立可上线，~30 分钟
- B：新建 `phase-labels.ts` 标签常量 — ~30 分钟
- C：新建 `PhaseStepper.tsx` + `OnboardingShell` 加 `userSelectedPhase` state + `jumpToPhase` 函数 — 2-3 小时
- D：新建 `ResumeOnboardingDialog.tsx` 弹窗 — 1-2 小时
- E：移动端响应式折叠 — 1 小时

### 待用户确认的未决问题
1. Phase 标签词汇是否符合预期
2. 跳回 phase0 改名字是否要级联重新生成 phase1_5 人格画像（建议不级联）
3. 弹窗触发条件是否同意"仅当 completedPhases.length > 0 时弹窗"
4. Phase2 修复方案 A（加强门控，推荐）还是 B（延迟解锁）
5. 实施顺序是否同意 A → B → C → D

**未修改任何代码，仅产出报告。等用户确认未决问题后再实施。**

---

## Phase2 修复方案变更：从"加强门控"改为"按 displayedAt 排序"

用户澄清需求：不希望禁止用户并发发送，而是希望 agent 回复**显示在最后一条消息下方**（像微信那样），不插中间。LLM 上下文管理不变。

### 新方案核心
- `ChatMessage` 加 `displayedAt` 字段（user=发送时间，assistant=揭示时间）
- 渲染时按 `displayedAt` 升序排序，替代数组 push 顺序
- 后端 `roleplay-agent.service.ts` 完全不动
- 改动 ~10-15 行，纯前端

### 已更新报告章节
- 摘要表格 #2 根因表述
- 1.2 节"用户反馈验证"加修复策略说明
- 2.3 节用户使用变化（从"禁用输入框"→"允许并发，按可见时间排序"）
- 3.4 节完整替换为"按 displayedAt 排序"方案（含 4 处改动 + 时间线验证 + 边界情况）
- 阶段 A 改动文件和验证更新
- 5.2 节从"并发性损失"改为"语义偏差"
- 六、#4 未决问题更新

报告路径：`outputs/onboarding-resume-and-phase2-chat-improvement-report.md`

---

## 用户确认未决问题 + 报告第四节扩充为实施手册

用户确认所有 5 个未决问题：
1. Phase 标签词汇符合预期
2. 跳回修改不级联 + 首次修改弹窗提示（新增 EditWarningDialog）
3. 弹窗触发条件 `completedPhases.length > 0`
4. Phase2 按 displayedAt 排序方案
5. 实施顺序 A → B → C → D → E

### 报告更新内容
- 5.1 节加"首次修改提示弹窗"设计（localStorage 标记一次性显示）
- 第六节标记所有未决问题为已确认
- **第四节大幅扩充**：每个阶段写成实施手册级别，包含：
  - 阶段 A：6 个步骤（类型扩展 + user/assistant 消息 displayedAt 赋值 + 揭示时更新 + 渲染排序 + 历史兜底），含完整代码前后对比
  - 阶段 B：2 个步骤（新建 phase-labels.ts 完整代码 + 可选重构）
  - 阶段 C：5 个步骤（PhaseStepper + EditWarningDialog 完整代码 + OnboardingShell 状态机升级 + advancePhase 改造 + 顶部布局），含 jumpToPhase/returnToLatestPhase 函数
  - 阶段 D：2 个步骤（ResumeOnboardingDialog 完整代码 + session 恢复 useEffect 改造），含弹窗回调
  - 阶段 E：3 个步骤（Stepper 窄屏折叠 + 两个弹窗底部抽屉式适配）

### 关键设计决策
- `userSelectedPhase` state 区分"用户主动跳回"vs"正常推进"，跳回时提交不推进而是 returnToLatestPhase
- advancePhase 改造：检测 userSelectedPhase !== null 时调 returnToLatestPhase 而非 nextPhase
- 弹窗触发后先 setPhase(session.phase) 避免空白屏，再让用户选择
- Stepper 仅在 phase !== 'finalize' && phase !== 'phase0' 时显示（与现有退出按钮逻辑一致）
- 首次修改提示用 localStorage 'echo_onboarding_edit_warning_shown' 标记，同用户只显示一次

**报告已就绪，等用户说"开始实施"即按 A → B → C → D → E 顺序执行。**

---

## 阶段 A 实施完成 — Phase2 聊天顺序修复（按 displayedAt 排序）

### 改动文件
1. `Echo/src/features/onboarding/v2/onboarding-v2.types.ts:210` — ChatMessage 加 `displayedAt: number` 字段
2. `Echo/src/features/onboarding/v2/Phase2Roleplay.tsx` — 7 处改动：
   - L76-101 `loadConversation`: 历史消息恢复时补全缺失的 `displayedAt`（旧数据用 timestamp 兜底）
   - L383-389 `initialMessages`: 初始 assistant 消息加 `displayedAt: Date.now()`
   - L413-419 `userMsg`: 加 `displayedAt: now`
   - L513-520 `assistantMsgs`: 加 `displayedAt: 0`（揭示时更新）
   - L565-576 setTimeout 回调: 揭示时 `setConversation` 更新 `displayedAt = Date.now()`
   - L828-838 渲染: 加 `.sort((a,b) => (a.displayedAt||a.timestamp) - (b.displayedAt||b.timestamp))`

### 验证
- `npm --prefix Echo run lint` 退出码 0（PosterPage 3 个已有报错不影响）
- 无新增 TS 错误

---

## 阶段 B 实施完成 — Phase 标签体系

- 新建 `phase-labels.ts`（PHASE_LABELS 常量 + 工具函数）
- Phase1_5Sketch / Phase1_6IdealSketch / Phase2Roleplay 的 h2 标题改为引用 PHASE_LABELS

---

## 阶段 C 实施完成 — 顶部 Stepper + 跳转 + 首次修改弹窗

### 新建 2 个组件
- `components/PhaseStepper.tsx` — 水平进度条，已完成 phase 可点击跳回；移动端折叠为"第 N/M 步"紧凑模式
- `components/EditWarningDialog.tsx` — 首次跳回修改提示弹窗 + `shouldShowEditWarning()` 工具函数（localStorage 一次性标记）

### OnboardingShell.tsx 改动
- 新增 state: `userSelectedPhase` / `showEditWarning`
- 新增 `jumpToPhase(target)` — 仅允许 target ∈ completedPhases；首次跳回触发 EditWarningDialog
- 新增 `returnToLatestPhase()` — 清空 userSelectedPhase，回最新 phase
- `advancePhase` 改造 — userSelectedPhase !== null 时调 returnToLatestPhase 而非推进；函数定义移到 returnToLatestPhase 之后（避免 TS2448）
- 顶部 sticky header 接入 PhaseStepper + X 按钮
- 用户跳回时显示黄色提示条"你正在修改已完成的内容 · 点击这里返回最新进度 →"

### 验证
- `npm --prefix Echo run lint` 退出码 0

---

## 阶段 D + E 实施完成

- 阶段 D：新建 `ResumeOnboardingDialog.tsx`，`OnboardingShell` session 恢复 useEffect 接入弹窗
- 阶段 E：两个弹窗改底部抽屉式（`items-end sm:items-center`），PhaseStepper 移动端已在 C1 实现

---

## 代码审查 + Bug 修复

审查所有改动后发现 2 个关键 Bug，已修复：

### Bug 1（关键）: 跳回 phase0 时 Stepper 消失
- **根因**: `OnboardingShell.tsx:252` Stepper 显示条件 `phase !== 'phase0'`，用户从 phase1 跳回 phase0 后 Stepper + 黄色提示条一并消失，用户卡住
- **修复**: 条件改为 `phase !== 'phase0' || userSelectedPhase !== null`

### Bug 2（关键）: handleExit 保存错误的 phase
- **根因**: `handleExit` L228 保存当前 `phase`（跳回时为 phase0），而非真实进度 `session.phase`（如 phase2）
- **修复**: `userSelectedPhase !== null` 时保存 `session?.phase ?? phase`

### 验证
- 修复后 `npm --prefix Echo run lint` 退出码 0

---

## 用户主页扩展 + 广场头像跳转 — 报告审查与执行前置

审查了 `outputs/user-profile-enhancement-and-avatar-navigation-report.md`（用户主页信息扩展 + 广场头像跳转改进），处理执行前 3 个质疑点 + 3 个改进点决策。

### 代码验证（2 个质疑点全部属实）
- **质疑点 1**：`block-filter.service.ts:26-39` 的 `getBlockedUserIds` 确为双向语义，`OR: [{ blockerUserId }, { blockedUserId }]` 两方向都查，注释明确"单向 block 产生双向隔离"。报告假设成立。
- **质疑点 2**：`common.module.ts` 是 `@Global()`，已 providers + exports `BlockFilterService`，`app.module.ts:54-55` 已导入。**profile.module.ts 不需要任何改动**，直接在 ProfileService 构造函数注入即可。报告中 A2.1 步骤"若未导入需加 imports"是多余动作，可跳过。

### 用户决策（4 项，修正报告原方案）
1. **UserProfileView 三区块顺序**：人格线索 → 理想型 → 广场动态（不是报告默认的"广场动态→人格→理想型"，理由：先看是什么人，再看期待谁，最后用作品佐证）
2. **广场动态"查看全部 N 篇"入口**：加（报告原方案无，跳转 `/feed?author={userId}`）
3. **RadarChart 默认 size**：240（报告原 200 可能标签拥挤，maxRadius 70→80）
4. **personaSketch 7 段叙事**：默认折叠，前 2-3 段展开（报告原方案全展开，700-1400 字信息过载）

### 后续执行修正清单（实施时务必遵守）
- profile.module.ts：不改 imports（@Global 已生效）
- UserProfileView 区块顺序：人格 → 理想型 → 广场动态
- 广场动态区块底部加"查看全部 N 篇 →"按钮
- RadarChart 默认 props size=240
- personaSketch 渲染：前 2-3 段默认展开，其余折叠 + "查看更多人格线索 →"
- 拉黑测试场景：A拉黑B / B拉黑A / 互拉 / 未拉黑 4 种组合（报告漏了"互拉"）
- mapPost 抽离后回归验证：用同一 postId 调 GET /v1/posts/:id，diff 响应 JSON 应为空

**报告方案整体合理可执行。等用户说"开始执行阶段 A"即按 A→B→C→D→E→F 顺序实施，应用上述修正。**
