
## Echo Logo v4 - Wordmark Design (00:00-00:03)
- Replaced plain Varela Round "Echo" text with AI-generated stylized wordmark
- Wordmark: rounded playful cartoon lettering, blue-purple gradient, pink heart accent on 'o'
- Layout: mascot top → wordmark middle → tagline "让 AI 替你遇见对的人" bottom
- New export: echo_wordmark.png (standalone wordmark)
- Updated: echo_logo.png, echo_logo.svg

## Echo Logo v5 - Integrated Agent x Love Typography (00:19-00:22)
- Replaced plain wordmark with integrated typography where mascot + hearts are WOVEN INTO the letters
- 'E' has sound wave/echo arcs in its strokes
- 'c' curves into a heart shape
- 'h' features the mascot character peeking from behind the vertical stroke
- 'o' is a gradient heart
- All letters share the blue-purple gradient with pink accent hearts
- Three ImageGen variants were generated; selected the one with mascot holding onto 'h'

## Echo Logo v6 - Clean Lockup (00:24-00:25)
- User wanted: (1) mascot consistency with standalone design, (2) simpler/cleaner, less cluttered
- Changed from vertical to horizontal lockup: mascot left + wordmark right + tagline below
- Mascot: used the SAME approved mascot (5:3) - no inconsistency
- Wordmark: minimal clean typography "Echo" with just one subtle heart replacing 'o'
- No characters, echo rings, or sound waves crammed into the letters
- Classic logo lockup layout, professional and clean

## Echo Mascot Icon Set - 14 Variations (00:29-00:35)
- Generated full icon set for Echo dating AI app mascot
- Set includes:
  * Gender variants: Boy (tuft of hair), Girl (bow + eyelashes)
  * Emotional expressions: Happy(default), Sweet, Wink, Heart-eyes, Surprised, Shy, Laughing, Cool, Thinking, Sleepy, Angry, Party, Proud
- All maintain consistent blue-purple gradient body with pink hearts and echo rings
- All saved to: Echo/logo/icons/ with numbered filenames 01-14
- File sizes: ~900KB-1.1MB per icon (1024x1024)

## Echo Logo Fix + Icon Set v7 — Consistent Design (00:39-00:47)
- Fixed corrupted logo files: re-exported all from Ardot canvas fresh
  - echo_logo.png (808KB), echo_mascot.png (555KB), echo_wordmark.png (180KB), echo_logo.svg (1.5MB)
- Regenerated 14 mascot icons with STRICT consistency to original design
  - Every prompt included identical character spec: teardrop blob, blue-purple gradient, round ears, 3 pink hearts, echo rings, sound wave arcs
  - Only expression changed (eyes + mouth); body/colors/background locked
  - Set: happy, boy, girl, wink, hearteyes, surprised, shy, laughing, cool, thinking, sleepy, angry, party, proud

## Echo Icons v8 — Round Face Fix (00:50-00:57)
- User reported v7 icons looked like mice (pointed snout) instead of round blob
- Root cause: ImageGen drifting to animal-like features without explicit constraints
- Fix: Added "NO snout, NO nose, NO mouse features, perfectly round ball-shaped face" to ALL prompts
- Also added "flat vector 2D, no shading" to maintain original flat style
- Regenerated all 14 icons with these constraints
- Face shape now consistently round/blobby, matching original echo_mascot.png

## Echo Icons v9 — Overlay-on-Original (01:02-01:09)
- Abandoned ImageGen (can't maintain character consistency from reference)
- New approach: Pillow overlay - draw new eyes/mouth ON TOP of original echo_mascot.png
- 01_happy = exact copy of original (zero changes)
- 02-14 = original base + drawn expression overlays covering original features
- Body, ears, hearts, echo rings, background = 100% identical across all icons
- Script: gen_icons.py in Echo/logo/

## Echo Icons v10 — Precise Feature Detection Regeneration (01:12-01:15)
- Rewrote icon generation with pixel-level facial feature detection instead of hardcoded % positions
- Script: analyze_and_gen.py in Echo/logo/
- Detection pipeline:
  1. Scanned all pixels in eye/mouth regions for dark clusters
  2. Used cluster centroid calculation for exact feature centers
  3. Fallback estimation when detection confidence is low (e.g., blush not present in original)
- Detected positions: Left eye (374,434), Right eye (515,434), Mouth (445,498)
- Eye draw radius uses original formula int(W*0.026)=23px for visual consistency
- All 14 icons regenerated: happy, boy, girl, wink, hearteyes, surprised, shy, laughing, cool, thinking, sleepy, angry, party, proud
- Quality verified via pixel sampling at detected feature coordinates (eyes, mouth)
- Overlay approach preserved: 100% original body/background, only expressions + accessories drawn on top

## Gender Icons v10 - Boy & Girl Expression Sets (16:37-16:44)
- Used echo_boy_wo_expression.png + echo_girl_wo_expression.png as base images
- Both are 2048x2048, no eyes/mouth pre-drawn
- Pillow overlay: drew 12 expressions on each base = 24 total icons
- boy: blue body + hair tuft, girl: pink body + bow
- Expressions: happy, wink, hearteyes, surprised, shy, laughing, cool, thinking, sleepy, angry, party, proud
- Output: Echo/logo/icons_gender/ (boy_01~12, girl_01~12)

## Echo 产品机制缺陷审计 (16:41)
- 对 Echo 全项目（services/api、services/worker、echo/、infra）做四路并行代码审计
- 报告输出：outputs/Echo-产品机制缺陷分析-2026-06-22.md
- 发现 11 项 P0 致命缺陷 + 30 项 P1 + 17 项 P2
- 关键 P0：OTP_DEV_CODE=123456 后门、session IDOR、.env 凭证入库、match/bridge 不过滤 block&dismiss、report-triage 不重审、agent-turn 非幂等、AuthShell 默认 OTP、WS token 不刷新
- 核心机制问题：Handoff 决策不校验 per-observer AffectionState（只看会话级 EMA）、Decay 衰减每日叠加（实际 7 倍速）、reciprocity 退化为自比、post-draft 绕过 composer 安全层、pause 后已入队任务仍执行
- 设计良好处：tension/repair arc、label 滞回、handoff 双方同意机制、affection event 幂等
- 结论：产品机制设计成立，但安全红线未守住、用户控制权（block）失效、精心设计被实现细节架空

## 缺陷修复分阶段方案设计 (23:13)
- 基于缺陷报告设计 7 阶段修复路线（阶段0-6）
- 文档：outputs/Echo-缺陷修复分阶段方案与AI提示词-2026-06-22.md
- 阶段0 凭证治理（运维）→ 阶段1 API安全红线（鉴权/OTP/IDOR/限流/审计）→ 阶段2 用户控制权全链路（block/dismiss/report）→ 阶段3 队列幂等+分身运行时 → 阶段4 核心机制正确性（Affinity/Handoff/Decay+DB迁移）→ 阶段5 客户端鉴权与会话 → 阶段6 客户端体验+数据校验
- 每阶段含：目标/覆盖缺陷/涉及文件/前置依赖/验收标准/可直接粘贴的AI提示词
- 关键发现：客户端实际路径是 Echo/src/（大写E），报告里写的 echo/src/ 需修正
- 依赖关系：0→1→{2,3→4,5}→6，阶段4含Prisma迁移风险最高

## 阶段0 凭证治理执行 (23:27)
- 任务：治理仓库凭证泄露（对应修复方案阶段0）
- 核查结论：实际 .env 未被 git 追踪（.gitignore 已生效），git 历史中 5 个密钥串 `git log -S` 全部无命中 → 历史无直接泄露
- 真实凭证散落位置：services/api/.env、services/worker/.env、services/worker/.env.bak、Echo/.env.local
- 发现两个不同的 DeepSeek Key：后端 sk-ef7e****（api/worker）、客户端 sk-32b8****（Echo/.env.local）
- 交付物：
  * 根目录新建 .env.example（统一模板，占位符+中文注释，OTP_DEV_CODE 留空）
  * 修正 services/api/.env.example 的 OTP_DEV_CODE=123456 → 空值（该后门曾推到 GitHub）
  * 加强 .gitignore（显式 .env.local/.env.bak/*.bak）
  * tmp/clean-env-history.sh（git filter-repo 预防性清理脚本，含备份+force push 提示，不自动执行）
  * outputs/密钥轮换清单.md（6 项需轮换：Neon密码/Upstash令牌/JWT_SECRET/2个DeepSeek Key/OTP后门，含步骤+更新位置+验证）
- 仍建议轮换：凭证散落+.env.bak备份+OTP后门暴露在远程，存在被复用风险

## 阶段0 凭证治理-执行第1批 (23:42)
- 用户要求执行：禁用生产OTP_DEV_CODE + 删除.env.bak + 轮换Neon/Upstash/JWT
- 已完成（本地可执行部分）：
  * OTP_DEV_CODE：services/api/.env 中 123456 → 置空；代码逻辑确认空值时后门分支不生效（auth.service.ts:142-143 空串为falsy）
  * services/worker/.env.bak：已永久删除（含凭证，不入回收站更安全）
  * JWT_SECRET：openssl rand -base64 48 生成新值，已更新 services/api/.env；worker 不使用 JWT_SECRET（grep 确认无引用）
- 待用户手动（无法代劳）：
  * Neon 数据库密码：需登录 console.neon.tech → Branches → Roles & Databases → Reset password（立即生效，旧连接断开）
  * Upstash Redis token：需登录 console.upstash.com → 数据库设置 → Reset Password（撤销旧 Standard/Read Only token）
  * 拿到新值后需同步更新 services/api/.env 与 services/worker/.env 的 DATABASE_URL 和 REDIS_URL
- 额外发现（代码缺陷，属阶段1）：auth.service.ts 无 NODE_ENV 防护，生产环境误设 OTP_DEV_CODE 仍会生效，建议加 `process.env.NODE_ENV !== 'production'` 守卫

## 密钥轮换清单调整 (2026-06-26 17:36)
- 用户决策：Neon PostgreSQL 密码与 Upstash Redis Token **不轮换**（经核查未泄露：无 git 历史/远程暴露，仅在本地 .env 中）
- 已更新 outputs/密钥轮换清单.md：Neon/Upstash 从"需轮换🔴"改为"不轮换❌"，新增第四节"不轮换项说明"列明理由与后续防护
- 当前剩余待办：2 个 DeepSeek Key 轮换（后端 sk-ef7e**** + 客户端 sk-32b8****）、协作者通知、生产部署核查
- 已完成项：JWT_SECRET 轮换、OTP_DEV_CODE 禁用、.env.bak 删除、.env.example 修正、.gitignore 加强

## DeepSeek API Key 轮换 (2026-06-26 17:51)
- 用户提供新 Key sk-4ffe****，采用"统一管理"方案：后端 + 客户端复用同一把新 Key
- 已更新三处 .env：services/api/.env、services/worker/.env、Echo/.env.local
- 旧 Key（后端 sk-ef7e****、客户端 sk-32b8****）待用户在 DeepSeek 平台手动删除
- 轮换清单 outputs/密钥轮换清单.md 已同步：DeepSeek 两项标记为已完成
- 至此阶段0凭证治理本地部分全部完成，剩余仅为平台侧手动删旧 Key + 协作者通知 + 生产部署核查

## 阶段1 API 安全红线修复 (2026-06-26 18:00)
- 一次性修复 9 个安全缺陷，tsc --noEmit 零错误通过
- 修改 13 个文件：
  * app.module.ts：JWT_SECRET 启动校验（>=32 字节，移除 'dev-secret-change-me' fallback）；注册 ThrottlerModule（全局 10/min）+ APP_GUARD ThrottlerGuard
  * auth.service.ts：OTP 后门加 NODE_ENV!=='production' 守卫；crypto.randomInt 替换 Math.random；token 注入 jti=randomUUID()；新增 logout() 方法（jti 入 Redis 黑名单 TTL=剩余有效期）；AuditService 注入，sendOtp/loginWithOtp 成功+失败均审计
  * jwt-auth.guard.ts：注入 RedisService，校验时查 jwt:blacklist:<jti> 黑名单，命中拒绝；改为 async canActivate
  * auth.controller.ts：/auth/otp @Throttle 3/min；/auth/login @Throttle 5/min；新增 POST /auth/logout
  * sessions.service.ts：messages() 加 clone 归属校验（session.cloneAId/cloneBId 须含 myClone.id），否则 NotFoundException
  * feed.controller.ts：加 @UseGuards(JwtAuthGuard)，@Throttle 20/min
  * llm-proxy.controller.ts：加 JwtAuthGuard 在 LlmProxyGuard 前
  * llm-proxy.guard.ts：req.user.id → req.userId（与 JwtAuthGuard 写入字段对齐）
  * matches.service.ts：注入 AuditService，dismiss→match_dismissed、block→user_blocked 审计
  * reports.service.ts：注入 AuditService，create→report_created 审计
  * posts.controller.ts：注入 AuditService，draft→post_drafted 审计；@Throttle 20/min
  * push.controller.ts：注入 AuditService，register→push_token_registered 审计；@Throttle 20/min
  * reports.controller.ts / onboarding.controller.ts：@Throttle 20/min
- package.json：+@nestjs/throttler ^5.1.0（实际装 5.2.0，--legacy-peer-deps）
- 关键发现：AuditService.log 实际签名是对象参数 {userId,cloneId?,eventType,referenceId?,summaryZh}，非位置参数
- 约束遵守：未改 Prisma schema、未改 worker、未改客户端

## Wan2.7-Image-Pro MCP Server 接入 WorkBuddy (23:59-00:18)
- 将阿里云通义万相 Wan2.7-Image-Pro 异步生图 API 封装为 WorkBuddy MCP 工具
- 产出：`mcp-wan-image/` 项目（server.py + dashscope_client.py + verify.py）
- 关键踩坑：DashScope 返回两种格式 — `output.results[].url`（旧）和 `output.choices[].message.content[].image`（Wan2.7 默认），需同时兼容
- venv 隔离：`~/.workbuddy/binaries/python/envs/wan-image`（mcp 1.28.0 + httpx 0.28.1）
- MCP 注册：`~/.workbuddy/mcp.json` 中配置 wan-image server，env 注入 DASHSCOPE_API_KEY
- 验证通过：1K 橘猫生图 → `outputs/images/b1e1811a..._0.png`（2.4MB），API Key 可用
- Skill：创建项目级 skill `wan27-mcp-image-gen`（`.workbuddy/skills/wan27-mcp-image-gen/`），记录完整工作流、API 格式差异、注册步骤
