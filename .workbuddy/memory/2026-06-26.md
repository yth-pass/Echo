# 2026-06-26 工作日志

## Echo 安全审计全链路修复（拉黑/忽略/举报控制权）

修复了安全审计发现的 6 个缺陷 + 1 个阻塞验收的既有 TS 问题，双服务 `tsc --noEmit` 均 exit 0。

### 关键改动
- **缺陷1**（worker `match-bridge.ts` `queryVectorCandidates`）：pgvector 候选 SQL 加双向 block 排除，复用 `$2`(seeker)，不消耗新参数位。
- **缺陷2**（worker `match-bridge.ts` `bridgeMatchPushes`）：findMany 加 `status: MatchPushStatus.pending`；按无向对键 `${a}|${b}` 内存过滤双向 block。
- **缺陷3**（worker `main.ts` report-triage）：改回 pending 后用原生 `new Queue('moderation',{connection})` 重新入队（按 post-draft 同款模式，非 API 的 QueueService）+ `report.remoderate` 审计。
- **缺陷4**（API 读取路径）：新增可复用 `BlockFilterService`（`common/block-filter.service.ts`）+ `@Global CommonModule`；应用到 feed(list/getOne)、sessions(listForUser)、matches(list，由单向改双向)。
- **缺陷5**（reports）：dto `targetType` 加 `@IsIn(['post','session','clone','user'])`；service `assertTargetExists` 校验目标存在性，不存在抛 NotFoundException。
- **缺陷6**（worker main.ts moderation 拒绝分支）：扩展 `WorkerPushType` 增 `moderation_rejected`；拒绝时 `sendPush` + `publishLiveEvent` feed 事件。
- **既有 TS1355**（worker main.ts agent-turn）：三元表达式 `as const` 非法，改 `as 'assistant'|'user'`（预存问题，非本次引入，阻塞验收故最小修复）。

### 约束遵守
- 未改 Prisma schema、未改客户端；每处加中文注释；BlockFilterService 可复用。
- 修改清单文档：`outputs/安全审计修复-修改清单.md`

### 注意事项（后续）
- MatchPushStatus 实际枚举值为 `pending/dismissed/accepted`（任务描述中写的是 `bridged`，实际无此值）；本任务按 `pending` 过滤。
- worker 入队用的是原生 BullMQ Queue（非 API 的 QueueService），新写 worker 逻辑时保持一致。

## Echo worker 幂等性与运行时健壮性修复（6 缺陷）

修复审计发现的 6 个 worker 缺陷（幂等性缺失 + 运行时健壮性），涉及 16 个文件，双服务 `tsc --noEmit` 均 exit 0。

### 关键改动
- **缺陷1**（agent-turn 幂等）：`queue.service.ts` `enqueueAgentTurn` 加 `turnIndex` 参数 + `jobId = agent-turn:${sessionId}:${turnIndex}`；`main.ts` processor 开头用 `turnId = ${sessionId}-${turnIndex}` 查 `AgentMessage`(@unique) 去重；自链入队也传 `nextTurnIndex` + `jobId`；创建消息时写 `turnId`。
- **缺陷2**（post-draft 走 composer）：`scheduler.ts` `generatePostContent` 改用 `composeSystemPrompt()`，boundary 通过 L1 安全层注入。
- **缺陷3**（persona 注入防护）：`prompt-composer.ts` 层级重排 L0 基础人设 → L1 安全边界 → 用户 persona → L2+ 技能/记忆；新增 `sanitizePersona()` 检测"忽略/ignore/disregard/system:"关键词，命中加隔离标记 + `<user_persona>` XML 包裹。
- **缺陷4**（LLM 失败处理）：`llm.ts` `chat()` 返回 `string | null`（缺 key / 重试耗尽均返回 null）；新增 `assertLlmKey()` 启动期 throw；`main.ts` agent-turn 中 null 时不写消息、`session.status=cancelled`、审计 + `publishLiveEvent`(新增 `session_error` 类型) + return；全量适配 9 个调用方（promote-check / unified-analysis / relationship-extract / relationship-upgrade-judge / topic-judge / social-extract / smoke-test-freechat / run-evals / llm-judge）。
- **缺陷5**（pause 跳过）：post-draft 和 agent-turn processor 入口查 `clone.status`，非 active 则跳过 + 审计 + return。
- **缺陷6**（forbiddenWords 后校验）：`boundaries.ts` 新增 `extractForbiddenWords` + `scanForbiddenWords`（大小写不敏感）；post-draft 生成后扫描，命中重新生成1次，仍命中则拦截。

### 注意事项
- `AgentSessionStatus` 枚举实际只有 `active/completed/cancelled`（无 `error`），缺陷4 暂用 `cancelled` 代替，待阶段4 schema 迁移加 `error`。
- `LiveEventType` 在 `live-event.ts`（代码文件，非 Prisma schema）中加了 `session_error`，不受"不改 schema"约束影响。
- `enqueueAgentTurn` 当前无 API 调用方（Phase 1 未接入），签名改了但无破坏性影响。
- 修改清单：`overview.md`

## Echo 核心机制修复（情感系统 DB 迁移 + handoff + bridge，9 步）

修复情感系统/Handoff/bridge 的 9 个核心机制缺陷，涉及 8 个代码文件 + 1 个迁移 SQL，双服务 `tsc --noEmit` 均 exit 0。

### 关键改动
- **步骤1**（schema 迁移）：新增 `AffectionState` per-pair 模型（`@@id([userAId,userBId])`，乐观锁 version，userAId<userBId 字典序）；Handoff 加 `expiresAt`；`HandoffStatus` 加 `expired`、`MatchPushStatus` 加 `bridged`。**关键决策**：AffectionState 不设 FK 到 User——调用方传 clone id（非 user id），FK 会拒绝写入；列名沿用 user_a_id 但存 clone id。
- **步骤2**（store 迁 DB）：`affection-state.store.ts` 重写为 Prisma upsert + 乐观锁（updateMany where version）+ pair 排序 + 一次性文件迁移（`migrateLegacyFile` 导入后重命名 .migrated）；read/write 签名不变。
- **步骤3**（decay 幂等）：新增 `reconstructBaselineDims(events)` 从不可变事件重建基线，`applyDecayFromBaseline` 目标值=基线-总衰减（基于 lastInteractionAt 重算），多次运行不叠加。
- **步骤4**（handoff 触发）：新增 `LABEL_RANK` + `bothLabelsHealthy(cloneA,cloneB)` 双向读 AffectionState，要求双方 label >= good_terms；条件改 `score>=0.75 && turnIndex>=4 && labelsHealthy`。
- **步骤5**（handoff 超时）：创建时设 expiresAt=now+7d；新增每小时 cron 扫 pending+过期→expired+通知+审计。
- **步骤6**（fallback）：`turnScore` 从 `0.5+turnIndex*0.05` 改固定 `0.5` + 审计。
- **步骤7**（reciprocity）：store 默认态 last_interaction_at 留空（识别新用户）；loadOtherWarmthDelta 读 (other,observer) store，不存在/无事件→null→multiplier=1.0。
- **步骤8**（handoff getOne）：移除 user_a_id/user_b_id，改 isUserA + contact_info 仅含对方 displayName+city。
- **步骤9**（bridge 去重）：bridge 前校验 push.status===pending，成功后立即置 bridged。

### 注意事项
- **迁移已落地 ✅**：`prisma migrate deploy` 成功应用 2 个 migration（`20260621000000_add_match_prefs_json` + `20260626110825_add_affection_state_and_handoff_expiry`）。根因是 .env 密码过期，更新后通过。
- AffectionState 键语义：当前存 clone id（列名 user_a_id 沿用任务约定，无 FK）。改 user id 键需同步改所有 read/write 调用点。
- repair_arc / tension_quality 未持久化（schema 未含），读取时默认。trust 修复弧线跨读不持久。
- 修改清单：`overview.md`

## Echo 客户端鉴权与会话安全修复（9 缺陷 + 验证补漏）

修复审计发现的客户端鉴权与会话管理 9 项致命缺陷，`cd Echo && npx tsc --noEmit` exit 0。修改清单：`outputs/auth-security-fix-changelog.md`。

### 关键改动
- **缺陷1**（AuthShell）：移除 `loginWithOtp(phone, code||'123456')` 默认值；code 为空 return 不发请求 + 按钮禁用。
- **缺陷2**（ws.ts）：token 不写 URL，改 WebSocket 子协议 `['bearer', token]`；连接前查 JWT exp 过期先 refresh；onclose 1008 不重连；重连强制重新取 token。
- **缺陷3**（client.ts）：`refreshOnce()` 单例 Promise 防并发；`request()` 内统一 401 拦截 refresh+重试一次，失败 `handleAuthFailure` 跳登录；match.ts 移除独立 refresh。
- **缺陷4**（App.tsx）：AbortController 取消 fetchMe（取代 cancelled 标志）；5s 超时 abort 进 auth；settled 标志保证 fetchMe 完成前不切状态；状态机 splash→auth/main/no-api。
- **缺陷5**（client.ts + 全部 api/*.ts）：定义 `ApiResult<T>` 四态联合类型；`request()` 返回 ApiResult；401 内部拦截不暴露；新增 `unwrap()` 兼容旧调用方；feed/match/session/handoff/clone/activity/audit/posts/report 全部适配。
- **缺陷6**（App.tsx + types.ts）：新增 `no-api` 状态；无 `VITE_API_BASE_URL` 时显示"未配置 API 地址"，不进登录/主界面。
- **缺陷7**（auth.ts + client.ts）：access token 存内存模块级变量；refresh token 不存 localStorage，依赖 httpOnly cookie（`credentials:'include'`，后端 TODO）；启动无 token 进登录页。
- **缺陷8**（audit.ts）：`raw==null` 返回 `[]`，`return mapped` 不回退 mock。
- **缺陷9**（auth.ts）：`parseJwtExp` + `scheduleProactiveRefresh` 定时器，token 剩余 5 分钟主动 refresh；登录/fetchMe/refresh 成功后调度；`clearProactiveRefresh` 登出清理。

### 验证补漏（tsc 首跑发现）
- `CloneView.tsx`/`ReportSheet.tsx`：判别联合跨文件 await 后窄化未生效，改 `in` 操作符守卫（`'reason' in result` / `'error' in res`）。
- `types.ts`：删除未使用的 `'checking'` 死代码状态。

### 后端 TODO（前端已预留）
- httpOnly cookie：登录接口 Set-Cookie，/auth/refresh 读 Cookie。
- WebSocket 子协议鉴权：后端从 Sec-WebSocket-Protocol 校验 `['bearer', token]`。

## Echo 客户端交互 + 后端校验缺陷修复（12 缺陷）

修复客户端交互体验（7）+ 后端数据校验（5）共 12 项缺陷。三处 `tsc --noEmit` 均 exit 0，prisma 迁移 `20260626122606_add_pending_review_status` 已应用。修改清单：`outputs/client-ux-backend-validation-fix-changelog.md`。

### 关键改动
- **客户端缺陷1/6合并**（App.tsx + main.tsx）：安装 react-router-dom@6.30.4；移除 selectedMatch/Post/Session 三 state，详情页改路由 `/post/:id` `/match/:id` `/session/:id`（互斥由路由保证）；main 状态用 `<Routes>` 替代 currentTab；F5 刷新保留路由。
- **缺陷2**（Onboarding.tsx sendDialogue）：res 为 null 时不加 fallback、不+1，显示错误让重试。
- **缺陷3**（Onboarding.tsx resetDialogueSession）：/dialogue/start 失败时 setDialogueReady(false)+return false，不掩盖。
- **缺陷4**（App.tsx + feed.ts/match.ts）：refreshFeed/refreshMatches 加 AbortController 取消旧请求；handleLive 200ms debounce 合并事件；loadFeed/loadMatches 新增 signal 参数。
- **缺陷5**（ws.ts）：重写 connectLiveEvents，localStorage lock 选举单 Tab 维护 WS + BroadcastChannel 广播；主 Tab 卸载释放 lock，其他 Tab storage 事件触发重新选举。
- **缺陷7**（ws.ts）：每 30s 发 ping，10s 无 pong 视为 stale 主动 close 重连；onmessage 处理 pong；cleanup 清理定时器。
- **缺陷8**（onboarding.service.ts）：finalize 入口校验 dialogueTurns>=MIN_TURNS（env 覆盖，默认4）抛 BadRequestException；已 finalize 抛 ConflictException。
- **缺陷9**（onboarding.dto.ts）：styleReplies/valuesChoices 加 @ValidateNested + @Type。
- **缺陷10**（matches.service.ts）：新增 buildMatchReasons 动态生成（同城/共同兴趣/向量相似度），从 bioJson.interests 取交集。
- **缺陷11**（worker main.ts + schema）：ModerationStatus 加 pending_review 枚举（已迁移）；needs_review 时置 pending_review + 入 manual-review 队列 + 审计 + live event。
- **缺陷12**（moderation.service.ts）：system prompt 追加隔离声明，user 消息用 `<content>` 包裹原文防注入。

### 注意事项
- react-router-dom 是新依赖（约束允许），已写入 package.json。
- BroadcastChannel 在不支持的浏览器降级为单 Tab 直连（try/catch）。
- WS 心跳与多 Tab 协调叠加：主 Tab 既维护心跳又维护 lock，卸任时 stepDown 统一清理。
- 后端 TODO：WS 需回 pong；manual-review 队列需后台审核消费方。

## 后端适配提示词整理

整理前几轮前端修复中标注的后端 TODO，基于实际后端代码确认，输出可直接发送给后端 AI 的具体提示词。文件：`outputs/backend-adaptation-prompt.md`。

### 4 项后端改动
1. **refresh token httpOnly cookie**：`auth.controller.ts` login/register/refresh 用 `res.cookie` 设置 httpOnly cookie，logout `clearCookie`；`main.ts` 加 `cookieParser()`；`auth.service.ts` `SnakeAuthResponse` 移除 `refresh_token`。当前后端从 body 读 refreshToken，前端已改 `credentials:'include'`。
2. **WS 子协议鉴权**：`live-ws.bootstrap.ts` 从 `request.headers['sec-websocket-protocol']` 解析 `bearer, <token>`，移除 `query.token`。前端已用 `new WebSocket(url, ['bearer', token])`。
3. **WS 心跳 pong**：`live-ws.bootstrap.ts` `wss.on('connection')` 加 `ws.on('message')` 收 `{type:'ping'}` 回 `{type:'pong'}`。前端已每 30s 发 ping。
4. **manual-review 消费 worker**：`services/worker/src/main.ts` 新增 `new Worker('manual-review', ...)`，至少日志+审计，供后续管理员界面接入。

### 关键确认点
- 后端 `/auth/refresh` 当前 `refresh(@Body() dto: RefreshDto)` → 需改 `@Req() req` 读 `req.cookies.refresh_token`
- WS `handleUpgrade` 无需额外传 protocols（ws 库自动回显）
- cookie `secure` 仅生产开启（本地 http 测试需 false）

## 后端安全适配 4 项落地（services/api + services/worker）

完成前端预留的 4 项后端适配，双服务 `tsc --noEmit` 均 exit 0。修改清单：`overview.md`。

### 关键改动
- **任务1 refresh token httpOnly cookie**：`main.ts` 加 `cookieParser()`；`auth.dto.ts` RefreshDto.refreshToken 改 `@IsOptional()`；`auth.service.ts` SnakeAuthResponse 移除 refresh_token，toSnakeAuthResponse 改 public，registerWithPassword/loginWithPassword 返回类型改 AuthSessionPayload（供 controller 拿 refreshToken 写 cookie）；`auth.controller.ts` login/register/refresh 注入 @Res 设 cookie（httpOnly/secure=生产/sameSite:strict/path:/v1/auth/maxAge:7d），refresh 优先读 req.cookies + rotation，logout clearCookie。新增依赖 cookie-parser + @types/cookie-parser。
- **任务2 WS 子协议鉴权**：`live-ws.bootstrap.ts` 移除 query.token，改从 `sec-websocket-protocol` 头解析 `bearer, <token>`，401 拒绝升级。URL 不再含 token，避免泄露 access log。
- **任务3 WS 心跳 pong**：`live-ws.bootstrap.ts` connection 内 `ws.on('message')` 收 `{type:'ping'}` 回 `{type:'pong'}`。
- **任务4 manual-review 消费 worker**：`worker/src/main.ts` 新增 `new Worker('manual-review', ...)` 复用 auditForClone + logger，写 `post.needs_review_queued` 审计；启动日志补 manual-review。

### 设计决策
- service 方法返回 AuthSessionPayload（含 refreshToken）而非 SnakeAuthResponse，因 controller 需 refreshToken 写 cookie；toSnakeAuthResponse 改 public 供转换出参。
- breaking change：refresh_token 从 body 移到 cookie（前端已适配 credentials:'include'）。
