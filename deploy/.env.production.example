# ==============================================================================
# Echo Production Environment Variables
# Copy to .env.production and fill in the actual values.
# ==============================================================================

# ---- Database (PostgreSQL + pgvector) ----
DB_NAME=echo_db
DB_USER=echo_user
DB_PASSWORD=CHANGE_ME__generate_a_strong_password
# Explicit connection URL (overrides compose-built URL).
# If DB_PASSWORD contains /, +, @, or #, use this instead
# with the raw password (Prisma tolerates unescaped special chars).
# DATABASE_URL=postgresql://echo_user:your_password@postgres:5432/echo_db?sslmode=disable

# ---- Redis ----
REDIS_PASSWORD=CHANGE_ME__generate_a_strong_password
# Explicit connection URL (overrides compose-built URL).
# If REDIS_PASSWORD contains /, +, @, or #, you MUST provide this
# with URL-encoded special characters (%2F for /, %2B for +, %40 for @).
# Example: redis://:pass%2Fword@redis:6379
# REDIS_URL=redis://:your_encoded_password@redis:6379

# ---- JWT ----
# Generate with: openssl rand -base64 64
JWT_SECRET=CHANGE_ME__generate_a_strong_random_string

# ---- AI / LLM APIs ----
DEEPSEEK_API_KEY=sk-your-deepseek-key
DASHSCOPE_API_KEY=sk-your-dashscope-key

# ---- Alibaba Cloud SMS (for phone OTP) ----
ALIBABA_CLOUD_ACCESS_KEY_ID=
ALIBABA_CLOUD_ACCESS_KEY_SECRET=
SMS_SIGN_NAME=
SMS_TEMPLATE_CODE=
SMS_CODE_VALID_MINUTES=5

# ---- Tencent Cloud COS (optional, for file uploads) ----
COS_SECRET_ID=
COS_SECRET_KEY=
COS_BUCKET=
COS_REGION=ap-shanghai

# ---- Frontend URL (public-facing URL) ----
# During ICP filing: http://124.223.73.232
# After ICP filing + SSL: https://your-domain.com
FRONTEND_URL=http://124.223.73.232
