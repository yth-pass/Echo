# Echo 仓库密钥轮换清单

> 生成时间：2026-06-22
> 最后更新：2026-06-26（Neon/Upstash 调整为不轮换）
> 用途：治理仓库凭证泄露问题，指导各平台密钥轮换与本地配置更新
> 评估对象：`services/api/.env`、`services/worker/.env`、`services/worker/.env.bak`（已删除）、`Echo/.env.local`

---

## 一、泄露评估结论

| 检查项 | 结论 |
|--------|------|
| 真实 .env 是否被 git 追踪 | ❌ 否（`.gitignore` 已生效，`.env`/`.env.local`/`.env.bak` 均未追踪） |
| git 历史是否曾出现真实密钥串 | ❌ 否（对 5 个密钥串执行 `git log --all -p -S` 均无命中） |
| 真实凭证是否在本地多文件散落 | ⚠️ 曾是（`.env.bak` 备份文件、`Echo/.env.local` 客户端配置均含真实凭证）—— `.env.bak` 已于 2026-06-22 删除 |
| 开发后门 OTP_DEV_CODE | ⚠️ `services/api/.env` 含 `OTP_DEV_CODE=123456`；`.env.example` 历史版本曾硬编码 `123456` 并推送到 GitHub —— 已于 2026-06-22 置空 |
| Neon PostgreSQL 密码是否泄露 | ❌ 否（未进 git 历史，未进 .env.example，仅在本地 .env 中） |
| Upstash Redis Token 是否泄露 | ❌ 否（同上，仅在本地 .env 中） |

**核心判断**：
- Neon / Upstash 凭证仅在本地 `.env` 中，未被 git 追踪、未出现在历史、未进入 `.env.example` —— **不构成泄露，无需轮换**。
- 仍需轮换的是：曾通过 `.env.example` 暴露在 GitHub 远程的 OTP 后门（已禁用）、本地生成的 JWT_SECRET（已轮换）、以及散落在 `Echo/.env.local` 的客户端 DeepSeek Key（Vite 打包后前端可见，属固有风险）。

---

## 二、密钥处置总表

| # | 密钥名称 | 当前值（脱敏） | 是否轮换 | 理由 | 状态 |
|---|---------|---------------|---------|------|------|
| 1 | Neon PostgreSQL 数据库密码 | `npg_****`（DATABASE_URL 内） | ❌ 不轮换 | 未泄露（无 git 历史/远程暴露） | 无需操作 |
| 2 | Upstash Redis 访问令牌 | `gQAA****`（REDIS_URL 内） | ❌ 不轮换 | 未泄露（同上） | 无需操作 |
| 3 | JWT 签名密钥 | `****`（已换新） | ✅ 已轮换 | 本地生成的签名密钥，纵深防御 | ✅ 2026-06-22 完成 |
| 4 | DeepSeek API Key（后端） | `sk-4****`（已换新） | ✅ 已轮换 | 统一使用新 Key，旧 Key 待删除 | ✅ 2026-06-26 完成 |
| 5 | DeepSeek API Key（客户端） | `sk-4****`（与 #4 统一） | ✅ 已轮换 | 复用后端 Key 统一管理 | ✅ 2026-06-26 完成 |
| 6 | OTP_DEV_CODE 开发后门 | （已置空） | ✅ 已禁用 | 曾通过 `.env.example` 暴露在 GitHub | ✅ 2026-06-22 完成 |

> ⚠️ 注意：#4 与 #5 是**两个不同的** DeepSeek API Key，必须分别轮换。

---

## 三、需轮换项操作步骤

### ✅ 3. JWT 签名密钥（已完成）

> 轮换会使所有已签发的 access/refresh token 立即失效，用户需重新登录。

**已完成操作（2026-06-22）：**
1. `openssl rand -base64 48` 生成新密钥
2. 更新 `services/api/.env` → `JWT_SECRET`
3. Worker 不使用 JWT_SECRET（已 grep 确认无引用，无需更新）

**验证：**
```bash
# 使用旧 token 请求应返回 401
curl -H "Authorization: Bearer <旧token>" http://localhost:4000/v1/me
```
- [x] `services/api/.env` → `JWT_SECRET` 已更新
- [x] 旧密钥 `Ji50****` 已失效

---

### ✅ 4. DeepSeek API Key（后端用，已完成）

**已完成操作（2026-06-26）：**
1. 用户提供新 Key `sk-4ffe****`
2. 更新 `services/api/.env` → `DEEPSEEK_API_KEY`
3. 更新 `services/worker/.env` → `DEEPSEEK_API_KEY`

**验证：**
```bash
# 用新 Key 测试 DeepSeek 接口
curl https://api.deepseek.com/v1/models \
  -H "Authorization: Bearer <新Key>"
```
- [x] `services/api/.env` → `DEEPSEEK_API_KEY` 已更新
- [x] `services/worker/.env` → `DEEPSEEK_API_KEY` 已更新
- [ ] 旧 Key `sk-ef7e****` 在 DeepSeek 平台删除（**待你手动操作**）

---

### ✅ 5. DeepSeek API Key（客户端用，已完成）

> 说明：原 `Echo/.env.local` 中的 `sk-32b8...` 与后端 Key 不同。本次按"统一管理"方案，客户端复用后端新 Key。
> ⚠️ 客户端暴露 API Key 本身存在风险（Vite 打包后前端可见），后续建议改为后端代理调用。

**已完成操作（2026-06-26）：**
1. 更新 `Echo/.env.local` → `DEEPSEEK_API_KEY`（复用后端新 Key）

- [x] `Echo/.env.local` → `DEEPSEEK_API_KEY` 已更新
- [ ] 旧 Key `sk-32b8****` 在 DeepSeek 平台删除（**待你手动操作**）

---

### ✅ 6. OTP_DEV_CODE 开发后门（已禁用）

> ⚠️ `123456` 曾通过 `.env.example` 暴露在 GitHub。生产环境若误启用，任意用户可用 `123456` 绕过短信/邮件验证码。

**已完成操作（2026-06-22）：**
1. `services/api/.env` 中 `OTP_DEV_CODE=123456` → 置空
2. `services/api/.env.example` 中 `OTP_DEV_CODE=123456` → 置空
3. 代码逻辑确认：`auth.service.ts` 中 `(devCode && code === devCode)` 空串为 falsy，后门分支不通过

**已更新位置：**
- [x] `services/api/.env` → `OTP_DEV_CODE=`（留空）
- [x] `services/api/.env.example` → `OTP_DEV_CODE=`（空值）
- [ ] 生产部署环境变量 → 确保不存在 `OTP_DEV_CODE` 或为空（**部署时核查**）

**验证：**
```bash
# 生产环境尝试用 123456 作为验证码，应被拒绝
curl -X POST http://<prod>/v1/auth/verify-otp \
  -d '{"code":"123456", ...}'
# 期望返回 400/401
```

**📌 纵深防御建议（属阶段1代码加固，非本轮运维范畴）：**
`services/api/src/auth/auth.service.ts` 缺少 `NODE_ENV` 防护，生产环境若误设 `OTP_DEV_CODE` 仍会生效。建议加守卫：
```ts
const devCode = process.env.NODE_ENV !== 'production'
  ? process.env.OTP_DEV_CODE?.trim()
  : undefined;
```

---

## 四、不轮换项说明

### Neon PostgreSQL 数据库密码

**不轮换理由：**
- ❌ 未进 git 历史（`git log --all -p -S "npg_e54QRENwuhvt"` 无命中）
- ❌ 未进 `.env.example`（4 个 .env.example 均为占位符）
- ❌ 未被 git 追踪（`.gitignore` 生效）
- ✅ 仅存在于本地 `services/api/.env`、`services/worker/.env`

**后续防护：**
- 保持 `.gitignore` 对 `.env` 的忽略
- 若未来发现任何泄露迹象（误提交、日志打印、截图外传），立即按原步骤在 [Neon 控制台](https://console.neon.tech) → Branches → Roles & Databases → Reset password 轮换

---

### Upstash Redis 访问令牌

**不轮换理由：**
- ❌ 未进 git 历史（`git log --all -p -S "gQAAAAAAAgOZ"` 无命中）
- ❌ 未进 `.env.example`
- ❌ 未被 git 追踪
- ✅ 仅存在于本地 `services/api/.env`、`services/worker/.env`

**后续防护：**
- 同上，保持 `.gitignore` 忽略
- 若未来发现泄露，按 [Upstash 文档](https://docs.upstash.com/redis/features/restapi) 在控制台 → 数据库设置 → Reset Password 轮换（会同时撤销旧 Standard/Read Only token）

---

## 五、清理动作完成情况

| 动作 | 命令/操作 | 状态 |
|------|----------|------|
| 删除 `services/worker/.env.bak` 备份文件 | `rm services/worker/.env.bak` | [x] 已完成（2026-06-22） |
| 确认 `.gitignore` 覆盖 .env/.env.local/.env.bak | 已加强（见 `.gitignore`） | [x] 已完成 |
| 确认 `.env.example` 无真实凭证 | 已生成根目录模板 + 修正子目录 | [x] 已完成 |
| OTP_DEV_CODE 生产后门禁用 | `.env` / `.env.example` 置空 | [x] 已完成 |
| JWT_SECRET 轮换 | `openssl rand -base64 48` | [x] 已完成 |
| 通知协作者重新配置本地 .env | 按新 `.env.example` 填写 | [ ] 待通知 |
| GitHub 仓库检查是否有 PR/fork 残留敏感历史 | Settings → 分支保护 / 联系 Support | [ ] 待核查 |

---

## 六、完成确认清单

- [x] ~~Neon 数据库密码轮换~~（不轮换，未泄露）
- [x] ~~Upstash Redis 令牌轮换~~（不轮换，未泄露）
- [x] JWT_SECRET 已更换，用户已知需重新登录
- [x] 后端 DeepSeek Key 已轮换，旧 Key `sk-ef7e****` 待平台删除
- [x] 客户端 DeepSeek Key 已轮换（复用后端新 Key），旧 Key `sk-32b8****` 待平台删除
- [x] OTP_DEV_CODE 生产环境已禁用
- [x] `services/worker/.env.bak` 已删除
- [ ] 所有协作者已用新凭证更新本地 .env
- [ ] 生产部署已完成配置同步并验证

---

> 📎 备注：DeepSeek 为第三方 SaaS 平台，轮换步骤基于平台通用控制台流程。如平台界面有变动，以平台官方文档为准。
> 📎 Neon / Upstash 经核查未泄露，本次不轮换；若后续发现泄露迹象，立即按四、五节指引操作。
